# Application Configuration
PORT=3000

# Secret key for salting visitor hash. generate in console with "node -e "console.log(require('crypto').randomBytes(32).toString('hex'))""
SECRET_KEY=your-secret-key-must-be-at-least-32-characters-long

# Flowcore tenant/organization name
FLOWCORE_TENANT=your-tenant-name
# this probably doesn't need to be changed
FLOWCORE_WEBHOOK_BASEURL=https://webhook.api.flowcore.io
# go to your tenants settings to generate api key
FLOWCORE_API_KEY=your-flowcore-api-key
# any key for local development, for prod generate key on platform
FLOWCORE_TRANSFORMER_SECRET=analytics-transformer-secret
# run the docker compose file. when you run project the table is created automatically
POSTGRES_CONNECTION_STRING=postgresql://postgres:postgres@localhost:5432/pathway_db